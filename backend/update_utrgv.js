require("dotenv").config(); const { Pool } = require("pg"); const pool = new Pool({ connectionString: process.env.POSTGRES_URL, ssl: { rejectUnauthorized: false } }); pool.query("UPDATE teams SET conference = 'Southland', school_primary_color = '#dc6000', stadium_name = 'Robert C. Vackar Stadium', stadium_city = 'Edinburg', stadium_state = 'TX' WHERE id = 3673").then(res => { console.log("Updated", res.rowCount); pool.end(); });
